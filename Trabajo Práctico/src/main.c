#include "main.h"

int main() {
    printf("Hola, mundo!\n");
    
    //int serverSocket = FUNCION()
    
    if (serverSocket == -1) {
        printf("Error creating socket");
        exit(EXIT_FAILURE);
    }
    
    while (TRUE) {
    
    
    
    }
    
    
    
    
    
    
    
    
    return 0;
}
